import React from 'react'
import styled from 'styled-components'

const Container = styled.div``

type Props = {
  children?: React.ReactNode
}

export const ExerciseForm: React.FC<Props> = () => {
  return <Container>ExerciseForm</Container>
}
